// Generated by CoffeeScript 1.10.0
(function() {
  var GamePrompt;

  GamePrompt = (function() {
    function GamePrompt() {}

    GamePrompt.prototype.promptFor = function(gameState, gameStatus) {
      if (gameStatus === "won") {
        return "Player " + (gameState.otherPlayer()) + " won.";
      } else if (gameStatus === "tied") {
        return "Y'all tied.";
      } else {
        return "It's player " + gameState.currentPlayer + "'s turn:";
      }
    };

    return GamePrompt;

  })();

  this.GamePrompt = GamePrompt;

}).call(this);
