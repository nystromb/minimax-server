// Generated by CoffeeScript 1.10.0
(function() {
  var GameState, PlayerMark;

  PlayerMark = {
    NONE: "_",
    X: "x",
    O: "o"
  };

  GameState = (function() {
    function GameState(board, currentPlayer) {
      this.board = board;
      this.currentPlayer = currentPlayer;
    }

    GameState.newGame = function() {
      var _;
      _ = PlayerMark.NONE;
      return new GameState([_, _, _, _, _, _, _, _, _], PlayerMark.X);
    };

    GameState.prototype.makeMove = function(space) {
      var newBoard;
      newBoard = this.board.slice();
      newBoard[space] = this.currentPlayer;
      return new GameState(newBoard, this.otherPlayer());
    };

    GameState.prototype.otherPlayer = function() {
      return this.currentPlayer === PlayerMark.X && PlayerMark.O || PlayerMark.X;
    };

    return GameState;

  })();

  this.PlayerMark = PlayerMark;

  this.GameState = GameState;

}).call(this);
